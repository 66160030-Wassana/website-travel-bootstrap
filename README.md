# website-travel-bootstrap
# git clone https://github.com/66160030-Wassana/website-travel-bootstrap
# git checkout -b development
# git checkout -b feature/index
# git add .
# git commit -m "ใส่ลิงไฟล์จาก bootstrap 5"
# git add .
# git commit -m "ใส่ menu,logo,ช่อง search"
# git add .
# git commit -m "แก้ไขสี menu,logo,search"
# git add .
# git commit -m "ใส่รูป recommend 3รูป" 
# git add .
# git commit -m "แนะนำสถานที่ที่เป็น highlight"
# git add .
# git commit -m "เพิ่มส่วน footer"
# git add .
# git commit -m "ปรับแต่ง footer"
# git add .
# git commit -m "เพิ่มส่วนหัวเรื่องหน้า details"
# git add .
# git commit -m "แนะนำสถานที่เที่ยว 1" 
# git add .
# git commit -m "แนะนำสถานที่เที่ยว 2"
# git add .
# git commit -m "แนะนำสถานที่เที่ยว 3"
# git add .
# git commit -m "แนะนำสถานที่เที่ยว 4"
# git add .
# git commit -m "แนะนำสถานที่เที่ยว 5"
# git add .
# git commit -m "แนะนำสถานที่เที่ยว 6"
# git add .
# git commit -m "แนะนำสถานที่เที่ยว 7"
# git add .
# git commit -m "แนะนำสถานที่เที่ยว 8"
# git add .
# git commit -m "ใส่ footer หน้า details"
# git add .
# git commit -m "เพิ่ม hamburger menu"
# git commit -m "แกไข hamburger menu"
# git add .
# git commit -m "ใส่ส่วนหัวใน contact" 
# git add .
# git commit -m "สร้างฟอร์มติดต่อและทำป้อปอัพเด้งขึ้น"
# git add .
# git commit -m "ใส่ footer หน้า contact"
# git add .
# git commit -m "รวบรวมคำสั่งทั้งหมด"